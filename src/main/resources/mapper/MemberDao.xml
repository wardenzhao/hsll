<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.thinkjoy.hsll.dao.MemberDao">
    <select id="getMemberByOpenId" resultType="cn.thinkjoy.hsll.bean.Member">
        select id as id, name as name, open_id as openId, sex as sex,address as address,invite_code as inviteCode
        FROM member
        WHERE open_id = #{openId}
    </select>

</mapper>

